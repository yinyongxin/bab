export enum AdmintorStatusEnum {
  Open = 'Open',
  Close = 'Close',
}

export const PermissionAuthorityEnum = {
  Create: 'Create',
  Delete: 'Delete',
  Update: 'Update',
  Query: 'Query',
} as const;

export enum SexEnum {
  Male = 'Male',
  Female = 'Female',
}
