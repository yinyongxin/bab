export enum StatusEnum {
  Open = 'Open',
  Close = 'Close',
}

export enum SexEnum {
  Male = 'Male',
  Female = 'Female',
}

export enum UserStatusEnum {
  Normal = 'Normal',
  Disable = 'Disable',
}

export enum MenuTypeEnum {
  Directory = 'Directory',
  Page = 'Page',
  FunctionArea = 'FunctionArea',
}
