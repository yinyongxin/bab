export enum AdmintorStatusEnum {
  Open = 'Open',
  Close = 'Close',
}

export enum PageAuthorityEnum {
  Create = 'Create',
  Delete = 'Delete',
  Update = 'Update',
  Query = 'Query',
}

export enum SexEnum {
  Male = 'Male',
  Female = 'Female',
}
