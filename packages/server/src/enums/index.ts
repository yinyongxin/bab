export enum StatusEnum {
  OPEN = 'open',
  CLOSE = 'close',
}

export enum SexEnum {
  MALE = 'male',
  FEMALE = 'female',
}

export enum MenuTypeEnum {
  DIRECTORY = 'directory',
  PAGE = 'page',
  FUNCTION_AREA = 'function-area',
}
